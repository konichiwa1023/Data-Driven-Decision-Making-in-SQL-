SELECT MIN(rating) AS min_rating,      -- Calculate the minimum rating
       MAX(rating) AS max_rating,      -- Calculate the maximum rating
       AVG(rating) AS avg_rating,      -- Calculate the average rating
       COUNT(rating) AS number_ratings -- Count the number of ratings
FROM renting
WHERE movie_id = 25; -- Select all records of the movie with ID 25
